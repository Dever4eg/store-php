<nav class="navbar fixed-top navbar-expand-md navbar-dark bg-primary">
    <a class="navbar-brand" href="#">DevStore</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="/">Products</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="cart">Cart</a>
            </li>
        </ul>

        <ul class="navbar-nav float-right">

            {% if not getUser() %}
                <li class="nav-item">
                    <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#login-modal">Login</button>
                </li>
            {% else %}
                <li class="nav-item">
                    {% if getUser().role =='admin' %}
                        <a href="/admin" class="navbar-text mr-sm-2">Admin panel</a>
                    {% else %}
                        <a href="/account" class="navbar-text mr-sm-2">Hello, {{ getUser().email }}</a>
                    {% endif %}
                </li>
                <li class="nav-item">
                    <a href="/logout" class="btn btn-primary">logout</a>
                </li>

            {% endif %}

        </ul>

    </div>
</nav>